"""Hermes plugin package for the GIS generative canvas (Phase 1).

The plugin entry point (``register(ctx)``) is added by Task 5 of the
Phase-1 plan; until then this file only marks the package so submodules
(validator, ops, store, tools_canvas) are importable.
"""
